Spark VM
-----

CentOS

General notes:
  AMI : spark-v1  (ami-dad09db2)
  login : ec2-user
  $ ssh     ec2-user@host_ip_address
  password : bingobob123
  or ssh key : hi1.pem

Pointers:
  - software is in /opt
  - env variables are set : ~/.bashrc
  - downloaded software in : ~/files
  - source code in ~/src

Starting AMI : es-clean-centos-vm-v6 (ami-9018abf8)

----
v1 : (ami-dad09db2)  @ 2015-02-05
----
- bring it up to date
    $   sudo yum update -y
    $   sudo reboot

- other
    $   sudo yum install -y s3cmd
    $   s3cmd --configure

- $   sudo chown ec2-user /opt

- java
  installed in /opt/java (points to right java)
  ~/.bashrc changes
      export JAVA_HOME=/opt/java
      export PATH=$JAVA_HOME/bin:$PATH

- scala  in /opt/scala
  sbt  in /opt/sbt

- edit ~/.bashrc
    export AWS_SECRET_ACCESS_KEY="PFaUQ8rmt5teqZEBXmZW1t+x9yOanN5vH+5N4DSk"
    export AWS_ACCESS_KEY_ID="AKIAIAKICCOIQVWUXT5A"

    export PATH=$JAVA_HOME/bin:/opt/scala/bin:/opt/sbt/bin:/opt/maven/bin:$PATH

-